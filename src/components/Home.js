import React from "react";
import MainApp from "../carousel/components/MainApp";

const Home = () => {
  return (
    <div className="home">
      <MainApp />
    </div>
  );
};

export default Home;
